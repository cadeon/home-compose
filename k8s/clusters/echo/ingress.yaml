apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: echo
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/proxy-http-version: "1.1"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/websocket-services: "echo"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - echo.k8s.not-really.me
        - api.echo.k8s.not-really.me
      secretName: echo-tls
  rules:
    - host: echo.k8s.not-really.me
      http:
        paths:
          # Dashboard routes (port 9119)
          - path: /
            pathType: Prefix
            backend:
              service:
                name: echo
                port:
                  number: 9119
    - host: api.echo.k8s.not-really.me
      http:
        paths:
          # API server routes (gateway port 8642)
          - path: /
            pathType: Prefix
            backend:
              service:
                name: echo
                port:
                  number: 8642
